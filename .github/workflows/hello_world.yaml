name: aws-hello2


on:
  workflow_run:
    workflows:
      - aws-deploy
    types:
      - completed

jobs:
  hello:
    runs-on: ubuntu-latest
    if: github.event.workflow_run.conclusion == 'success'
    steps:
      - name: Checkout
        uses: actions/checkout@v1

